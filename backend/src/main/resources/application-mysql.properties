# MySQL defaults (override with DB_URL/DB_USER/DB_PASS)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/demomanagement_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:}
